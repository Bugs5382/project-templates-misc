name: GolangCI
on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    branches:
      - main
jobs:
  lint:
    name: GolangCI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: GolangCI
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.10.1
          args: --timeout=3m
